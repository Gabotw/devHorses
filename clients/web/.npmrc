; PrimeNG 21 (última versión gratis/MIT, sin la licencia comercial "PrimeUI" de v22)
; declara peer @angular/common@^21, pero funciona sobre Angular 22 (app más nueva que la lib).
; Esta bandera evita que `npm install` falle por ese conflicto (también en Vercel/CI).
legacy-peer-deps=true
